using System;
//If money is left in a particular bank for more than 5 years, the interest rate given by the bank is 7.5%,
//else the interest rate is 5.4%. Write a program that prompt the user for the number of years
//that the money was left in the bank and display the appropriate interest rate depending on the value input.
//How many runs should you make to very that it works correctly? 
namespace Lab2_If_statements__301194572
{ 
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Number of years the ");
                
            

        }
    }
}

































            //Console.WriteLine("How many hardcover books: ");
            //int hardCover = Convert.ToInt32(Console.ReadLine());

            //double averagePrice = 125;
            //const double plusBooksDiscount = 0.05;
            //const double premium = 0.2;

            //if (hardCover > 0 || hardCover <= 4)
            //{
            //    double totalCost = averagePrice * hardCover;
            //    double finalCost = (totalCost * premium) + totalCost;
            //    Console.WriteLine($"Cost without discount: {finalCost:c}");

            //    if (hardCover > 4)
            //    {
            //        totalCost = (averagePrice * hardCover);
            //        finalCost = totalCost + (totalCost * premium) - (totalCost * plusBooksDiscount);
            //        Console.WriteLine($"Cost with discount: {finalCost:c}");
            //    }
            //}
            //if (hardCover == 0)
            //{
            //    Console.WriteLine("How many softcover books: ");
            //    int softCover = Convert.ToInt32(Console.ReadLine());
            //    double totalCost = averagePrice * softCover;
            //    Console.WriteLine($"Cost without discount: {totalCost:c}");

            //    if (softCover > 4)
            //    {
            //        totalCost = averagePrice * softCover;
            //        double finalCost = totalCost - (totalCost * plusBooksDiscount);
            //        Console.WriteLine($"Cost with dicount: {finalCost:c}");
            //      

